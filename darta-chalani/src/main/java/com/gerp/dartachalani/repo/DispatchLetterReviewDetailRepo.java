package com.gerp.dartachalani.repo;

import com.gerp.dartachalani.model.dispatch.DispatchLetterReviewDetail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DispatchLetterReviewDetailRepo extends JpaRepository<DispatchLetterReviewDetail, Long> {
}
